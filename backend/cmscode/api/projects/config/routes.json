{
  "routes": [
    {
      "method": "GET",
      "path": "/projects",
      "handler": "projects.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/projects/:id",
      "handler": "projects.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}