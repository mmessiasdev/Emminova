{
  "routes": [
    {
      "method": "GET",
      "path": "/team",
      "handler": "team.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/team/:id",
      "handler": "team.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}